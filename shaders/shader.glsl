#version 150

// UNIFORM VARIABLES
//uniform mat4 projModelViewMatrix;
//uniform vec3 cubletPos;


void main(){
	// gl_Position = projModelViewMatrix * (vec4(cubletPos, 1.0) + gl_Vertex);
//	gl_Position = gl_Vertex;

}

